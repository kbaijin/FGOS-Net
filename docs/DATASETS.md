# Datasets

Datasets are not redistributed in this repository.

The paper evaluates crack benchmarks and cross-domain thin-structure datasets
using external dataset roots on the server. Public code should not contain private
absolute paths, server addresses, or copied dataset files.

When training/evaluation scripts are released later, dataset paths should be
passed through command-line arguments or environment variables rather than being
hard-coded.
